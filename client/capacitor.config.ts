import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourcompany.elysianshores',
  appName: 'Elysian Shores',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
